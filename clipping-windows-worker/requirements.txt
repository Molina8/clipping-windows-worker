# Core
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0

# Logging estructurado (opcional pero recomendado)
structlog>=24.1.0

# Transcripción (GPU)
# Adaptado a la API de whisperx 3.8+ (fork mantenido).
# Instalar con soporte CUDA: pip install whisperx
whisperx>=3.8.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
